+ нумерация таблиц

+ задавать переменные в начале программы а не в каждой функции по 100 раз

+ если задано нулевое число колонок — (ТАБЛИЦЫ НЕ ВЫВОДЯТСЯ) "Неправильное число колонок!" ($tr_num ==0)
	+ если в таблице нет строк — (таблица не выводится) "В таблице нет строк!" ($strusture[$i] == '') (нет ни # ни * ни содержания == '')
		+ если в таблице нет строк с ячейками (все строки пустые) — (таблица не выводится) "В таблице нет строк с ячейками!" ($null_tr == count($tr)) ('#') (нет *)
			+ если есть пустая строка (без ячеек) — (строка пропускается) её HTML-код не выводится

+ починить 10 таблицу (связано с последним пунктом тут)


'' - нет строк (не задано число строк)
'#' - нет ячеек (не задано число столбцов/ячеек)
'C1*C2##C5*C6' - в строке нет ячеек

'##'

tr
''
''
''

td
''
''
''

'C1*C2##'

tr
'C1*C2'
''
''

td
'C1' 'C2'
''
''

						ЛАБА ГОТОВА!!! (сделать коммит и выложить на сервер, начинать следующую лабу)